
export enum Role {
     USER = "USER",
     ADMIN = "ADMIN"
}

export enum UserStatus {
     ACTIVE = "ACTIVE",
     INACTIVE = "INACTIVE",
     BANNED = "BANNED"
}

export enum OTPStatus {
     PENDING = "PENDING",
     VERIFIED = "VERIFIED",
     EXPIRED = "EXPIRED"
}
